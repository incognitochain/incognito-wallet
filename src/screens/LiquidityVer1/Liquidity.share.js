import React from 'react';
import withShareVer1 from '@screens/LiquidityVer1/Liquidity.enhanceShare';

const ListShare = React.memo(() => {
  return null;
});

export default withShareVer1(ListShare);